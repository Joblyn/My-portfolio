import { RouteConfig } from "@/interfaces/route";

const HOME: RouteConfig = {
  path: "/",
  name: "home",
};

const BLOG: RouteConfig = {
  path: "/blog",
  name: "blog",
};

export { HOME, BLOG };
